<%= form_for(@xml_sample) do |f| %>
  <% if @xml_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@xml_sample.errors.count, "error") %> prohibited this xml_sample from being saved:</h2>

      <ul>
      <% @xml_sample.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
